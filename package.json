{
  "name": "mememaker",
  "version": "1.0.0",
  "description": "Meme Maker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "invoke": "sls invoke -f mememaker --logger --path mocks/request.json",
    "invoke:local": "sls invoke local -f mememaker --docker --logger --path mocks/request.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@hapi/joi": "^17.1.1",
    "axios": "^0.25.0"
  }
}
